const readline = require('readline');
const rl = readline.createInterface({
    input: process.stdin,
    output: process.stdout
});
var inputArr = [];
rl.on('line', function (input) {
    inputArr.push(input);
    var m = +inputArr[0].split(' ')[0];
    var n = +inputArr[0].split(' ')[1];
    for(var i=0;i<t;i++){
        if (inputArr.length == (m+n+1)) {

        }
    }
});